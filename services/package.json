{
  "name": "devomain-services",
  "version": "1.0.0",
  "description": "services for devoMain",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel . --presets babel-preset-env --out-dir . --ignore node_modules",
    "prepublish": "npm run build",
    "release": "npm publish --tag next"
  },
  "keywords": [
    "nodejs",
    "es6"
  ],
  "author": "devo li",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-env": "^1.6.1"
  },
  "dependencies": {
    "aws-sdk": "^2.148.0",
    "extend": "^3.0.1",
    "nodemailer": "^4.3.1",
    "nodemailer-ses-transport": "^1.5.1"
  }
}
